import SwiftUI

extension Color {
    static let royalBlue = Color(red: 0.08, green: 0.72, blue: 0.65)     // Teal
    static let hermesOrange = Color(red: 0.96, green: 0.62, blue: 0.04)  // Amber

    // Adaptive colors for dark mode support
    static let cardBackground = Color(.secondarySystemBackground)
    static let surfaceBackground = Color(.systemGroupedBackground)
}
